import Quick
import Nimble

class ExampleSpec: QuickSpec {
    override func spec() {
        describe("Test Spec") {
            it("should execute and fail this test") {
                expect(true) == false
            }
        }
    }
}
